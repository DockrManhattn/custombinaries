#!/bin/bash

if [ -z "$1" ]; then
    echo "Usage: $0 <base64_encoded_text>"
    exit 1
fi

encoded_text="$1"
decoded_text=$(echo "$encoded_text" | base64 -d | tr -d '\0')

echo "Decoded PowerShell text:"
echo "$decoded_text"
